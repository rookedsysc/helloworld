spring:
  docker:
    compose:
      enabled: false
  sql:
    init:
      mode: never

jwt:
  issuer: kafka-az-test
  secret: kafka-az-test-secret-key-must-be-at-least-32-bytes
  access-token-expiration-seconds: 3600

outbox:
  relay:
    enabled: false
